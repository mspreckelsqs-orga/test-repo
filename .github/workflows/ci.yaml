name: CI

on:
  workflow_dispatch:
 
jobs:
  print-env:
    runs-on: ubuntu-latest
    environment: ${{ github.ref_name }}
    env:
      DUMMY: ${{ secrets.DUMMY }}
    steps:
      - name: print
        run: |
          printenv
